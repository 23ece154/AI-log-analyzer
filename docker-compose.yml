version: "3.8"

services:

  backend:
    build: ./backend
    container_name: ai_log_backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: ai_log_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend